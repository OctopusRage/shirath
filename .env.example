# ClickHouse Configuration
CH_CONNECTION=http://default:password@localhost:8123/your_database
CH_CONNECTION_SLAVE=http://default:password@localhost:8123/your_database

# PostgreSQL Configuration
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=your_database
DB_HOST=localhost

# CDC (Change Data Capture) Configuration
CDC_PUBLICATION=your_publication_name
CDC_SLOT=your_slot_name

# Mapper Configuration (optional - defaults to mapper.json)
MAPPER_FILE=mapper.json

# Sentry (optional - for error tracking)
# SENTRY_DSN=https://your-sentry-dsn
